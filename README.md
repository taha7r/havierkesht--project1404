# havierkesht--project1404
# پروژه پایانی درس برنامه‌نویسی وب - سامانه هاویرکشت 🌾
**دانشگاه لرستان - نیمسال اول ۱۴۰۴** **استاد:** دکتر آرمین رشنو  
**دانشجو:** طاها رحیمی

##  اهداف پروژه
این سامانه با هدف رصد و پایش محصولات و صورتحساب‌های مالی کشاورزان چغندرقند طراحی شده است. قابلیت‌های کلیدی بررسی شده در این پروژه شامل مدیریت وزن بار، عیار، درصد افت و سهمیه قند و تفاله است.

##  مرحله ۱ و ۲: مستندسازی و تحلیل APIها
لیست APIهای کلیدی که از محیط Swagger استخراج و تست شده‌اند:

![محیط Swagger و تست APIها](images/api-test.png)

| متد | مسیر (Endpoint) | توضیح |
| :--- | :--- | :--- |
| `POST` | `/users/admin/` | ایجاد کاربر جدید با نقش ادمین |
| `GET` | `/seed/` | بازیابی لیست دانه‌ها با قابلیت فیلتر |
| `PUT` | `/farmer/{id}` | به‌روزرسانی اطلاعات کشاورزان |
| `DELETE` | `/product-price/{id}` | حذف قیمت محصول |

**اتصال صفحات به API:**
- **صفحه لاگین:** متصل به `GET /users/{id}` جهت احراز هویت.
- **صفحه ثبت سم:** متصل به `GET /pesticide/` و `GET /factory/`.

![مانیتورینگ درخواست‌ها در تب Network](images/api-check.png)

##  مرحله ۶ تا ۹: زیرساخت و استقرار خودکار
در این پروژه از زیرساخت‌های ابری برای میزبانی استفاده شده است:

* **میزبانی (Hosting):** استفاده از پلتفرم ابری **لیارا** (Liara).
* **مدیریت نسخه:** استفاده از **GitHub** برای Push کردن سورس‌کد.
* **CI/CD:** پیاده‌سازی فرآیند استقرار خودکار؛ به طوری که با هر `git push` تغییرات بلافاصله روی سرور اعمال می‌شود.

![پنل لیارا و تایید استقرار خودکار](images/liara-panel.png)